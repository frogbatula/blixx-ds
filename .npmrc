# Cloudflare Pages sets NODE_ENV=production during install.
# Keep Vite/TypeScript available, and skip Playwright browser downloads
# (those are only needed for local Storybook vitest runs).
production=false
engine-strict=false
fund=false
playwright_skip_browser_download=true
